{
  "name": "franco-hq",
  "version": "0.1.0",
  "private": true,
  "description": "Franco HQ — single read-only pane of glass over every marketing/comms tool. Zero deps, registry-driven, cannot drift.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
